#ifndef INSTANT_RUNOFF_VOTES_H
#define INSTANT_RUNOFF_VOTES_H

#include <vector>
#include <string>

std::vector<std::string> election(const std::vector<std::string>& candidates, const std::vector<std::vector<unsigned int>>& ballots);

#endif
